import { AXSecurityDomain } from "../run/AXSecurityDomain";



export function describeTypeJSON(sec: AXSecurityDomain, value: any, flags: number) {
    return describeTypeJSON(sec, value, flags);
  }
  